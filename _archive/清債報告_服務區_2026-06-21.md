> 文件用途：官網首頁服務區塊（手風琴／開發卡／沙盒）累積技術債的稽核結果與清理待辦（2026-06-21 23:50 多代理 workflow 產出）
> 退場條件：A 區安全項全清完 + B 區創辦人決策項拍板並執行後失效
> 歸檔位置：失效後移至 `_archive/`

# 清債報告：首頁服務區塊（2026-06-21 23:50）

> **2026-06-23 22:32 執行註記（本輪清債）**：A 區大半已執行（commit `99ecba9`）——
> ① 刪除 8 個被取代的根目錄預覽/沙盒頁（`index-rain-preview.html` 等）。**⚠ 本報告內凡稱「現役正本＝index-rain-preview.html」均已過時**：正式入口現為整合後的 `index.html`（手風琴＋堆疊玻璃＋落雨皆已併入）。
> ② 退役 6 個死服務 teaser 頁（social/virtual/website/system/consultant/tiktok）；about/news/growth 三孤兒頁依文件刻意保留。
> ③ 重建 sitemap.xml（只列 /、blog、contact）；④ `.gitignore` 補本機暫存（launch.json／_nocache_server.py／_imgtest/_*.py）；⑤ demo-liquid 沙盒納管。
> **仍未處理（退場條件未全滿足，暫不歸檔）**：`_imgtest/` 52 張未追蹤探索圖（創辦人決定「先不動」）、about/news/growth 去留、CLAUDE.md §四 多處仍指 index-rain-preview 為現役的過時敘述（主文件債，待徵詢後另批處理）。

來源：6 條稽核線 workflow（已實測 git 追蹤狀態、預覽頁實際引用、部署設定）。現役正本＝`index-rain-preview.html`；軟體開發現役沙盒＝`demo-liquid/service-glass-webgl-real.html`。

## ⚠️ 兩個最重要（先處理）

1. ✅ **已解決（2026-06-22 06:14）**：創辦人拍板＝**三項服務堆疊打光玻璃**（沙盒 `demo-liquid/service-glass-stack.html`、commit `442b261`）。§四 已收斂為單一現役段、另兩段（攝影棚 06-21／堆疊藝術卡 06-15）標退役。原稽核保留如下 ↓
   - ✅ **落雨追加修復（2026-06-22 23:36，創辦人驗收通過）**：現役 `index-rain-preview.html #dev` 落雨曾分岔壞掉（內縮暗卡＋中央擋雨），已逐項對齊確定版 `demo-liquid/service-glass-codeRain.html` 修復（補回邊緣羽化遮罩等）。詳見 CLAUDE.md §四「軟體開發卡落雨」段＋ memory。
   - **開發卡「三套並存、文件打架」（高風險）**：CLAUDE.md §四 同時有舊段（堆疊藝術卡＋程式碼星雨，06-15 定案）與新段（WebGL 攝影棚玻璃，06-21）描述開發卡、未交叉聲明取代；而**實際 `index-rain-preview.html` 跑的是第三套（CSS 玻璃面板，WebGL 已用 `if(true){return}` 停用）**。→ 下個 session 會照不同段落往相反方向做。~~必須創辦人先拍板：開發卡最終是「堆疊藝術卡＋碼雨」還是「WebGL 攝影棚玻璃」？~~ 已拍板（見上）。
2. **現役定案圖卡在測試夾 `_imgtest/`、且 4 個沙盒未納 git（手滑即永久遺失）**：①現役 5 張定案圖（mural-j／social-mix-a2／blog4-b／ads2-a／ai-particle-a-sq）在 `_imgtest/`（測試夾）非 `assets/images/`，正式整合前須搬遷改路徑納 git，否則部署排除 `_imgtest` 會破圖。②4 個未追蹤沙盒（`demo-liquid/_cta-variants.html`、`service-glass-compare.html`、`service-glass-css-bevel.html`、`service-glass-webgl-stack.html`）不在 git 保護傘下，**先 `git add` 再決定去留、勿直接 rm**。

## A. 安全可立刻清（低風險）

- `_imgtest/_keyout*.py`（3 個本機去背腳本、含寫死路徑、無引用）→ `.gitignore` 加 `_imgtest/_keyout*.py`，檔留本機。
- `.claude/launch.json`（本機 server 設定）→ `.gitignore` 擴 `.claude/`。
- `index-rain-preview.html` 第 452–604 行 ≈147 行被 `if(true){return}` 封死的 Three.js 玻璃整段（含 `window.__dg` 死碼）→ 刪（git 可取回）。
- `index-rain-preview.html` `.glass-fallback` 死 DOM（注入 445–451＋三段 CSS）→ 連同上條一起刪。
- `index-rain-preview.html` 第 378 行 誤導註解（描述已不存在的水卡 iframe）→ 刪。
- `index-rain-preview.html` `?v=liquid2` cache-bust（22–24 行，退役液態實驗殘留）→ 移除、對齊正式 index。
- `.assetsignore` 補排除 `_imgtest/`、`demo-liquid/`、`index-*-preview.html`、`preview-accordion-*.html`（`pages deploy .` 現會把 141MB 原圖＋53 沙盒 html 推上正式站）→ **前提：正式頁不可再依賴 `_imgtest` 路徑（先做圖搬遷，見上 #2）**。
- `demo-liquid/` 約 40+ 已否決探索檔（水卡全批／3D 液體 D／液態 F·V／flow／星球 3D／fusion）→ 已追蹤、`_index.html` 已編目、**原地留作探索檔案庫即可，不需逐檔刪**。

## B. 需創辦人確認再動（中高風險）

- §四 開發卡雙段落消歧（見 ⚠️#1）。
- `service-glass-webgl-real.html` vs `service-dev-stack-art.html` 兩條開發卡方向未收斂、各有 keep 標記 → 形式拍板後留一條。
- 4 個未追蹤沙盒（見 ⚠️#2）→ 先 commit 納管再決定封存/刪。
- `service-glass-webgl-pop.html`（CLAUDE.md 明文封存版、checkpoint 709101b 已留底）→ 確認後可不留檔。
- `blog.html`（已改 +163/-26 未 commit）＋ `blog-archive.html`、`assets/css/pages/blog*.css`、依賴 `_imgtest/arc-*.png`/`blog-*.png` → blog prototype 進行批次，確認要進基線後**圖文同批 commit**（漏圖會 commit 破圖頁）。
- `_imgtest/` 約 57 張未追蹤探索變體圖（mural-c/d/f/g、ai-v2-*、expand-*、social-* 等，無引用、各 3–4MB）→ 移本機 `_imgtest_archive/` 或徵詢後刪；**勿一刀 gitignore 整個 `_imgtest/`**（會與已追蹤的 19 張衝突、定案圖更新被靜默漏）。
- `index-planets-preview.html`（星球方向舊預覽、已被 rain 取代）+ 其第 169 行 iframe 載 `service-accordion-embed.html` → 確認星球退役後封存/刪，退 embed 須同步處理此引用。

## C. 保留／現役（別動）

- `index-rain-preview.html` 內嵌 `.svc-accordion`（手風琴唯一正本）、`#dev` 碼雨區。
- `demo-liquid/service-glass-webgl-real.html`（開發卡定稿沙盒 commit 93e3db4、stage2 在此進行）。
- `demo-liquid/service-accordion-embed.html`（仍被 4 個 `preview-accordion-*`＋`index-planets-preview` iframe 引用，退役須**同批**處理所有 iframe）。
- `service-dev-stack-art.html`、`_index.html`、`showcase-all-cards.html`（資產源／導覽）。
- CLAUDE.md §四 手風琴收合卡段／展開態段（雙份 active 紅線為防呆紀律）。
- 正式 `index.html`（最新 2190f33、工作區乾淨、未被預覽線誤動）。

## 一句話總評
債主要兩處：**①開發卡三套並存、文件互相打架（必須創辦人先拍板形式，否則持續分叉）②`_imgtest/` 半追蹤灰色地帶（現役圖卡測試夾、57 張中介圖未納管、部署又整包推上線）**。最該先做：§四 開發卡消歧 + 4 個未追蹤沙盒先 `git add` 防手滑，之後封存與部署排除才有乾淨基準。

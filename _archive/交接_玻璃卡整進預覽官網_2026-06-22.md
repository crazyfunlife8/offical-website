> 文件用途：軟體開發「玻璃卡堆疊」沙盒整進預覽官網 index-rain-preview.html 的交接單（含 codex 已出的整合方案、接手須知、驗證踩坑）。
> 退場條件：玻璃卡成功整進 index-rain-preview.html `#dev` 區、Chrome 驗收通過（玻璃卡對＋手風琴沒壞）後失效。
> 歸檔位置：失效後移至 `_archive/`。

# 交接：玻璃卡堆疊整進預覽官網（2026-06-22 06:45）

## 一句話現況
軟體開發卡的 WebGL 玻璃堆疊**設計已定稿並 commit**（只在沙盒裡）；**下一步＝把它整進預覽官網 `index-rain-preview.html` 的 `#dev` 開發區**。整合方案 codex 已出（見下），尚未執行。

## 已完成（本批次）
- **開發卡設計定稿**：沙盒 `demo-liquid/service-glass-stack.html`，commit **`442b261`**（本機未 push）。
  - 定案＝「聚光主卡發光水晶 ＋ 非聚光卡全暗零反光 ＋ 退場卡捲動淡出消失」。
  - selective bloom（layer-based 雙 composer，只有聚光主卡進 bloom）；envMap body/edge 分離（body 乾淨、studio 長條只在 edge）；背景程式碼透過玻璃折射（codeGlow）。
  - 定稿 `window.__fx` 預設：heroEdgeGlow **1.45**、bloomStrength **0.26**、codeGlow 0.05、rearFrost 1、bloomThreshold 0.985、bloomRadius 0.035。
  - 創辦人定稿前卡打光 `state`/`LIGHTS`（源 commit `93e3db4`）原封不動。
- **文件收斂**：CLAUDE.md §四 開發卡三段打架→收成一段（堆疊玻璃為唯一現役、另兩段標退役），清債報告 ⚠️#1 標已解決，commit **`0c4c8e6`**。
- **memory 同步**：`project_nestdigital_dev_glass_card.md` ＋ MEMORY.md 索引已改成現役定案。

> repo HEAD 現為 `0c4c8e6`（其上 `0049c69` 是別的 session 提交的 blog 批次、非本批次）。兩個本批次 commit 都**未 push**。

## 下一步 = #5 整進預覽官網（codex 已出方案，待執行）

**目標**：把 `service-glass-stack.html` 的 WebGL 玻璃 module 原生整進 `index-rain-preview.html` 的 `#dev` 區，用**頁面捲動**驅動。**不是**整進正式 index.html（那是整個預覽定案後的後話）。

**codex 方案重點**：
1. **嵌入**：原生內嵌、開專用 namespace `.dev-glass-stack`（比照手風琴前綴隔離手法，但別用 `.svc-accordion`）。DOM：`#dev > .dev-glass-stack#devGlassStack > .dev-stack-track(scroll長度) > .dev-stack-stage(sticky 100vh) > canvas.dev-stack-webgl + .dev-stack-html-deck`。canvas/HTML deck 在 stage 內 `absolute inset:0`——WebGL **鎖在 #dev 區段內、不 fixed 到全站**，不蓋 hero／手風琴／CTA。JS 全部從 `root=getElementById('devGlassStack')` 往下查；全域只留 `window.__fx`。
2. **Three.js 並存**：預覽頁現有 **r128 global**（給星空/nav，不可拔）。玻璃卡走 **r0.160 importmap module**，兩版並存、module 不准碰 `window.THREE`。代價＝多載一份 Three（預覽可接受）。
3. **清掉舊碼**：`#dev-rain`(Canvas2D 碼雨，玻璃自帶 codeTexture 已重複)、`#dev-glass`(已停用 WebGL，且含 PointLight 違紅線)、`.glass-fallback`、`__dg` 死碼、舊 `.tunepanel`、`#dev .inner` CSS 假玻璃——刪。順手把 #dev 現存 mojibake 中文換成定稿中文（CARDS 資料取自沙盒）。
4. **移除舊 GSAP**：`#dev .scard` 的舊 sticky/opacity ScrollTrigger 要移除，否則跟新 `layoutStack()` 打架（codex 抓到的坑）。
5. **debug 面板預設隱藏**：?debug=1 或按 H 才出；`window.__fx` 永遠掛著。
6. **照搬定稿**：上述 __fx 定稿值、selective bloom、envMap 分離、非聚光卡全暗、退場淡出、state/LIGHTS 不動。
7. **不准碰**：`.svc-accordion` 手風琴、hero/manifesto/final CTA、assets/js 全站腳本。整合點只在 #dev HTML/CSS/JS ＋ 新增 importmap/module。
8. **效能**：IntersectionObserver 只在 #dev 進 viewport 才 render；30fps 節流；手機降階（低 DPR、低 codeTexture 更新率、bloom 關/極低）。

**創辦人已認可此方案、並指示執行**——接手後可直接請 codex 動手（先別再重出方案）。

## 接手須知

- **codex thread（有完整玻璃卡＋整合方案上下文）**：`019eebae-d3fa-7841-9ea7-3f0c0475bb9c`。新對話可用 `mcp__codex__codex-reply` 帶此 threadId 續接，省去重講。sandbox=workspace-write、cwd=`NEW創巢官網`。
- **本機驗證 server**：`python -m http.server 5511`（在 `NEW創巢官網` 目錄起）。網址 `http://localhost:5511/index-rain-preview.html`。
- **WebGL 驗證踩坑（血淚，務必照做）**：
  - 分頁失焦會被瀏覽器節流→rAF 停→畫面凍住、截圖卡死。驗證前先在 Chrome console 跑可見度覆寫讓 render 不停：
    ```js
    Object.defineProperty(document,'hidden',{configurable:true,get:()=>false});
    Object.defineProperty(document,'visibilityState',{configurable:true,get:()=>'visible'});
    document.dispatchEvent(new Event('visibilitychange'));
    ```
  - 沙盒 `?qa=1` 會把 stack progress **鎖死在 0**（只顯第一張），看不到捲動切換——驗證捲動要**不帶 qa**、改用上面覆寫＋`window.scrollTo` 捲到位＋等十幾幀再截圖。
  - 同 URL 會吃快取看到舊畫面→驗 codex 改動**必加 cache-bust**（`?cb=隨機`）。
  - 玻璃卡 debug 面板按 **H** 收／開。
  - 驗收用瀏覽器分頁工具（claude-in-chrome），別用 computer-use 全螢幕截圖。
- **紅線（玻璃卡通用）**：禁 PointLight/DirectionalLight、禁人工描線高光/sparkle/band/ticks（靠材質＋envMap＋Fresnel）；純黑冷藍底、金 1%、不灌暖金；創辦人定稿 state/LIGHTS 不動。
- **codex 改檔風險**：codex 編碼環境會把整檔中文存成 mojibake／逕改英文——每次改完**必 grep/讀回自查中文**（網站架設／客製化系統開發／數位轉型顧問／服務範疇／九個服務子項／HTML title）。
- **commit 政策**：此 repo 部署走 Cloudflare wrangler，中文 commit body 觸發 8000111 卡部署→**commit 首行＋body 一律純 ASCII 英文 conventional commit**。用 `git-commit-push` agent。只 commit 不 push（除非創辦人說 push）。
- **驗收標準**：玻璃卡三張捲動切換正常（主卡發光、後卡全暗、退場淡出）＋ **手風琴沒被弄壞** ＋ 其他區塊（hero/CTA/星空）正常。

## 任務清單狀態（harness task 不跨對話、記在此）
- ✅ #1 對齊上輪進度、釐清文件偏移
- ✅ #2 基礎三修（後片鎖暗／小字對比／中央收版）
- ✅ #3 craft 升級（冷光水晶定稿，commit 442b261）
- ✅ #4 CLAUDE.md §四 收斂（commit 0c4c8e6）
- 🔲 **#5 整進預覽官網 index-rain-preview.html `#dev` 區（方案已備、待執行）← 新對話從這裡接**
- （後話，非現在）整個預覽定案後→搬正式 index.html（5 層 CSS）＋ 5 張定案圖 `_imgtest`→`assets` 納版控
